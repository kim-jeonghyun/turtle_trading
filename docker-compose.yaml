services:
  # 시그널 체크 서비스 (supercronic 기반, non-root)
  turtle-cron:
    build: .
    container_name: turtle-cron
    user: "${DOCKER_UID:-1000}:${DOCKER_GID:-1000}"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import pathlib, time; logs=sorted(pathlib.Path('/app/logs').glob('*.log'), key=lambda f: f.stat().st_mtime, reverse=True); exit(0 if logs and time.time() - logs[0].stat().st_mtime < 86400 else 1)"]
      interval: 5m
      timeout: 10s
      retries: 3
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - turtle-network

  # Streamlit 대시보드
  turtle-dashboard:
    build: .
    container_name: turtle-dashboard
    user: "${DOCKER_UID:-1000}:${DOCKER_GID:-1000}"
    command: streamlit run app.py --server.port=8501 --server.address=0.0.0.0
    ports:
      - "127.0.0.1:8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - turtle-network
    depends_on:
      - turtle-cron

networks:
  turtle-network:
    driver: bridge
